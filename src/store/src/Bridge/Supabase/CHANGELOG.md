CHANGELOG
=========

0.14
----

 * Add `count` method

0.11
----

 * Normalize the `endpoint` URL and tolerate a trailing slash
 * [BC BREAK] Rename the `$url` constructor/factory argument to `$endpoint`

0.7
---

 * Add support for `Store::remove()` method

0.1
---

 * Add the bridge

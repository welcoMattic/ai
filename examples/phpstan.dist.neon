includes:
    - ../.phpstan/extension.neon

parameters:
    level: 6
    paths:
        - ./
    excludePaths:
        - vendor
    ignoreErrors:
        -
            message: "#^Method .*::test.*\\(\\) has no return type specified\\.$#"
            path: tests/*

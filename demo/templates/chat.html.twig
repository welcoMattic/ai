{% extends 'base.html.twig' %}

{% block body_class 'chat-page ' ~ chat %}

{% block content %}
    <div class="chat-page-content" style="--demo-gradient: {{ demos[chat].gradient }}">
        {{ component(chat) }}
    </div>
{% endblock %}

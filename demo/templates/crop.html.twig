{% extends 'base.html.twig' %}

{% block body_class 'chat-page crop' %}

{% block content %}
    <div class="chat-page-content" style="--demo-gradient: {{ demos.crop.gradient }}">
        {{ component('crop') }}
    </div>
{% endblock %}
